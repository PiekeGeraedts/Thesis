In pygcn_Kemeny:
-The adjacency matrix returned by utils is not symmetrically normalized. For now that doesn't matter, we don't use these values anyway.

Else:
-add JB's Markov_Chain module to required installments in setup.py
-add files to '.git/info/exclude' that you do not want to include in git, for example personal files can be usefull to add here instead of to '.gitignore'.

